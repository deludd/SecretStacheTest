import React from 'react';
import { Spinner, LoadingContainer } from '../styles/LoadingSpinnerStyles';

const LoadingSpinner = () => (
  <LoadingContainer>
    <Spinner />
  </LoadingContainer>
);

export default LoadingSpinner;
